# География для школьников 🗺️

Интерактивная образовательная игра по географии для школьников, написанная на C++ с использованием библиотеки SFML.

## Описание

Игра представляет собой викторину с вопросами о географии: столицах стран, крупнейших реках, горах, пустынях и других географических объектах. Игрок должен выбрать правильный ответ из четырех предложенных вариантов.

## Особенности

- 🎯 15+ вопросов по географии
- ⏱️ Таймер на каждый вопрос (30 секунд)
- 📊 Система подсчета очков
- 🎨 Приятный визуальный интерфейс
- 🖱️ Интуитивное управление мышью
- 🔄 Возможность перезапуска игры

## Требования

- C++ компилятор с поддержкой C++17 (g++, clang++)
- CMake версии 3.10 или выше
- SFML 2.5 или 3.0 (поддерживаются обе версии)
  - **Примечание:** Игра протестирована с SFML 3.0.2. При использовании SFML 2.5 могут потребоваться небольшие изменения в коде.

## Установка SFML

### macOS (через Homebrew)
```bash
brew install sfml
```

### Linux (Ubuntu/Debian)
```bash
sudo apt-get install libsfml-dev
```

### Windows
1. Скачайте SFML с официального сайта: https://www.sfml-dev.org/download.php
2. Распакуйте архив
3. Укажите путь к SFML в CMakeLists.txt или используйте переменные окружения

## Сборка и запуск

### Сборка проекта

```bash
mkdir build
cd build
cmake ..
make
```

### Запуск игры

**На macOS:**
```bash
open build/GeographyGame.app
```

Или напрямую:
```bash
./build/GeographyGame.app/Contents/MacOS/GeographyGame
```

**На Linux/Windows:**
```bash
./build/GeographyGame
```

Или из директории build:
```bash
cd build
./GeographyGame
```

## Использование

1. Запустите игру
2. Прочитайте вопрос
3. Выберите правильный ответ, кликнув на одну из четырех кнопок
4. Правильный ответ подсветится зеленым, неправильный - красным
5. После ответа автоматически появится следующий вопрос
6. В конце игры вы увидите ваш итоговый счет
7. Нажмите "Играть снова" для новой игры

## Структура проекта

```
Игра/
├── main.cpp          # Основной файл с кодом игры
├── CMakeLists.txt    # Файл конфигурации CMake
└── README.md         # Документация
```

## Добавление новых вопросов

Чтобы добавить новые вопросы, отредактируйте функцию `initializeQuestions()` в файле `main.cpp`:

```cpp
questions.push_back(Question(
    "Ваш вопрос?",
    {"Ответ 1", "Ответ 2", "Ответ 3", "Ответ 4"},
    индекс_правильного_ответа // 0, 1, 2 или 3
));
```

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Создано для изучения географии школьниками.

